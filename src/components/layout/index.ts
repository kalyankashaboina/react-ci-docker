/**
 * Layout Components
 *
 * This directory contains structural components that define the application's layout.
 * Examples include Header, Footer, Sidebar, Navigation.
 *
 * Principles:
 * - Layout components should be application-specific and reusable
 * - Keep layout logic separate from business logic
 * - Compose layout components with common components
 * - Co-locate styles and tests with each component
 *
 * Component Structure (co-location):
 * Header/
 *   ├── Header.tsx
 *   ├── Header.module.scss
 *   └── Header.test.tsx
 *
 * Import Pattern:
 * - For single components: import Header from './Header/Header'
 * - For multiple components: use index.ts in each component folder
 */

// Import layout components directly from their folders
// import Header from './Header/Header';
// import Footer from './Footer/Footer';
// import Sidebar from './Sidebar/Sidebar';
